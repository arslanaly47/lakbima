= form_for dynamic_menu, html: { class: "form-horizontal" } do |f|
  = render "shared/errors", resource: dynamic_menu
  .form-group
    = f.label :name, class: 'col-sm-2 control-label'
    .col-sm-10
      = f.text_field :name, class: 'form-control'
  .hr-line-dashed
  .form-group
    = f.label :description, class: 'col-sm-2 control-label'
    .col-sm-10
      = f.text_area :description, class: 'form-control'
  .hr-line-dashed
  .form-group
    = label_tag "dynamic_menu[from_account_type_ids]", "From Account Lists", class: "col-sm-2 control-label"
    .col-sm-10
      = select_tag "dynamic_menu[from_account_type_ids]", options_for_select(AccountType.all.map { |a| [a.name, a.id] }), class: "form-control", multiple: true
      %span.help-block.m-b-none
        Note: You can also choose more than one Account List in the section.
  .hr-line-dashed
  .form-group
    = label_tag "dynamic_menu[to_account_type_ids]", "To Account Lists", class: "col-sm-2 control-label"
    .col-sm-10
      = select_tag "dynamic_menu[to_account_type_ids]", options_for_select(AccountType.all.map { |a| [a.name, a.id] }), class: "form-control", multiple: true
      %span.help-block.m-b-none
        Note: You can also choose more than one Account List in the section.
  .hr-line-dashed
  .form-group
    .col-sm-4.col-sm-offset-2
      = f.submit "Save", class: "btn btn-primary"
      = link_to "Cancel", account_types_path, class: "btn btn-primary"
