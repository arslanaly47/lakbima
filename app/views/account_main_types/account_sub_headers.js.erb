$("#accountSubTypeSelectTag").html("<%= j options_for_select(@account_sub_types.map { |a| [a.name, a.id] }) %>");
$("#accountTypeSelectTag").html("<%= j options_for_select(@account_types.map { |a| [a.name, a.id] }) %>");
